<%= form_with url:"/#{current_user.username}/follows", method: :post do |form| %>
  <%= form.hidden_field :"follow[username]", value: followee.username %>
  <% unless list.include?(followee.username) %>
    <%= form.submit "Follow" %>
  <% end %>
<% end %>
