<hr>
<div>
  <p>
   <%= account.full_name %> <%= link_to "@#{account.username}", user_path(account) %>
  </p>
  <%= render "follows/follow_button", followee: account, list: @usernames_followed %>
</div>